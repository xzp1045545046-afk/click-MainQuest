# 任务手册（Todo Manual）— 完整复刻规格说明书

> 本文件是一份**自包含的产品 + 技术规格**。把整份文档交给任意 AI Coding 应用，即可一比一复刻出这个网站当前的全部效果，无需任何历史对话。
> **不包含**任何用户已输入的任务数据（todos 内容为空数组起步）。

---

## 1. 产品定位

一个**单机本地运行的游戏风任务管理手册**，视觉主题致敬《地下城与勇士》（DNF）的暗色金边 UI，并内置一套《星露谷物语》田园亮色皮肤。核心特征：

- **单文件 HTML**：全部 CSS / JS / SVG 图标内联在一个 `.html` 文件中（仅字体文件外置）。
- **双主题**：暗色 DNF 风（默认）⇄ 亮色星露谷风，一键切换，偏好持久化。
- **本地数据**：任务存 localStorage，并通过一个本地 Python 同步服务写入 `data.json` 文件，实现多入口（浏览器/桌面快捷方式/预览面板）数据互通。
- **像素字体**：中文使用 Fusion Pixel 12px 像素体，暗色用等宽版、亮色用比例版，均按 latin/汉字子集分包加载。
- **无框架、无构建**：原生 HTML + CSS + JS（ES2020），零依赖，直接双击或经本地服务打开即可运行。

---

## 2. 页面结构与布局

### 2.1 整体布局（三栏书本式）

```
┌──────────────────────── main ────────────────────────┐
│  titlebar（标题栏：任务手册 + 右侧 × 关闭装饰）        │
│  quote（每日完成数毒舌文案，居中金色小字）             │
│ ┌────────┬──────────────────────┬──────────────────┐ │
│ │ rail   │ page                 │ detail-panel     │ │
│ │ 15%    │ 45%                  │ 40%              │ │
│ │ 头像   │ content-title        │ detail-head      │ │
│ │ 导航   │ list（任务列表）      │ detail-body      │ │
│ │ 主题   │ add-todo-btn         │  (hint+textarea) │ │
│ │ 切换   │ composer（抽屉表单）  │ detail-actions   │ │
│ └────────┴──────────────────────┴──────────────────┘ │
└───────────────────────────────────────────────────────┘
                外框：浏览器居中的"窗口"卡片
```

- 视口：`body` 用 flex 水平居中，内容包在 `.scale-wrap`（`width:min(1600px, calc(100vw - 24px))`，上边距 28px）。
- `main` 是仿窗口外框（暗色：深蓝黑底 + 多层 box-shadow 描边；亮色：羊皮纸底 + 3px 木框 + 圆角 10px）。
- `.book` 用 CSS Grid 三栏：`minmax(0,15%) minmax(0,45%) minmax(0,40%)`，最小高度 570px。
- 窄屏适配：`@media(max-width:1000px)` 时 `.book` 变单列，rail 与 detail-panel 的左右边框改为上下边框。

### 2.2 HTML 骨架（必须保持的 DOM 结构与 id）

```html
<body>
<div class="scale-wrap">
<main>
  <header class="titlebar">任务手册
    <span class="close" title="关闭">[× SVG]</span>
  </header>
  <div class="quote" id="dailyStat"></div>
  <div class="book">
    <aside class="rail">
      <div class="crest"><img src="[头像]" alt="头像"
           style="width:100%;height:100%;object-fit:cover;display:block"></div>
      <nav id="nav"></nav>
      <button class="theme-toggle" id="themeToggle" title="切换明暗模式">
        <svg class="ico-sun">…</svg><svg class="ico-moon">…</svg>
        <span id="themeLabel">亮色模式</span>
      </button>
    </aside>
    <div class="page">
      <h2 class="content-title" id="contentTitle"></h2>
      <ul class="list" id="list"></ul>
      <button class="add-todo-btn" id="addTodoBtn" title="添加新任务">
        <svg><path id="plusV" d="M8 3v10"/><path d="M3 8h10"/></svg>添加新任务
      </button>
      <form class="composer" id="form">
        <p class="form-label">接受新任务</p>
        <div class="new-todo">
          <input id="text" maxlength="15" required placeholder="输入任务内容（最多15字）">
          <input id="note" maxlength="5" placeholder="备注（可选）">
        </div>
        <div class="meta">
          <div class="major-picker">
            <label><input type="radio" name="major" value="主线" checked>主线任务</label>
            <label><input type="radio" name="major" value="支线">支线任务</label>
          </div>
          <button class="add">接受任务</button>
        </div>
      </form>
    </div>
    <aside class="detail-panel" id="detailPanel">
      <div class="detail-head"><span class="detail-title" id="detailTitle">点击任务查看详情</span></div>
      <div class="detail-body">
        <p class="detail-hint">※ 在此记录任务攻略、备注或相关描述，输入自动保存</p>
        <textarea class="detail-text" id="detailText"
                  placeholder="选中任务后在此输入大段文字..." disabled></textarea>
        <div class="detail-actions" id="detailActions">
          <button class="act" id="editBtn" disabled>编辑</button>
          <button class="act" id="delBtn" disabled>删除</button>
        </div>
      </div>
    </aside>
  </div>
  <div class="modal-overlay" id="editModal">
    <form class="modal-panel" id="editForm">
      <p class="form-label">编辑任务</p>
      <div class="new-todo">
        <input id="editText" maxlength="15" required placeholder="输入任务内容（最多15字）">
        <input id="editNote" maxlength="5" placeholder="备注（可选）">
      </div>
      <div class="meta">
        <div class="major-picker">
          <label><input type="radio" name="editMajor" value="主线">主线任务</label>
          <label><input type="radio" name="editMajor" value="支线">支线任务</label>
        </div>
      </div>
      <div class="modal-btns">
        <button type="button" class="ghost" id="editCancel">取消</button>
        <button type="submit" class="add">确认修改</button>
      </div>
    </form>
  </div>
</main>
</div>
<script>…全部逻辑…</script>
</body>
```

要点：

- **两个 `<style>` 块**：第一个为暗色（默认）主题全量样式；第二个开头补充 rail/add-todo-btn/theme-toggle/composer 等共享样式，随后用 `body.light …` 选择器覆盖出亮色主题。
- 所有图标均为**内联 SVG**（关闭 ×、太阳/月亮、十字、编辑铅笔、删除垃圾桶、排序手柄六点），禁止外链图片；头像 `<img>` 可换任意本地像素图。
- favicon 用 **data URI SVG** 内嵌（暗色金框蓝黑底 / 亮色木框奶油底两套），JS 切换主题时同步替换。

---

## 3. 文案规范（逐字复刻）

| 位置 | 文案 |
|---|---|
| 标题栏 | `任务手册` |
| 导航-主标签 | `进行中任务` / `已完成任务` |
| 导航-子标签 | `主线任务` / `支线任务` / （动态）完成日期如 `2026/08/19` |
| 内容标题 | `进行中任务` / `主线任务` / `支线任务` / `已完成任务 · {日期}` |
| 空列表 | `暂无任务` |
| 添加按钮 | `添加新任务`（展开后变 `暂不添加`） |
| 表单标题 | `接受新任务`，提交按钮 `接受任务` |
| 输入占位 | `输入任务内容（最多15字）` / `备注（可选）` |
| 单选 | `主线任务` / `支线任务` |
| 详情默认标题 | `点击任务查看详情` |
| 详情提示 | `※ 在此记录任务攻略、备注或相关描述，输入自动保存` |
| 详情占位 | `选中任务后在此输入大段文字...` / 已完成视图 `已完成任务详情只读` |
| 操作按钮 | `编辑` / `删除` |
| 编辑弹窗 | `编辑任务` / `取消` / `确认修改` |
| 删除确认 | `确定要删除这项任务吗？`（confirm） |
| 主题按钮 | 暗色时显示 `亮色模式`，亮色时显示 `暗色模式`（反显目标态） |

### 3.1 每日完成数毒舌文案（粤语，随机一条）

完成数 n = 当天 `done && completedAt === 今日` 的任务数，显示在顶部 `.quote`（id=`dailyStat`）。日期用 `new Date().toLocaleDateString('zh-CN',{year:'numeric',month:'2-digit',day:'2-digit'})`，北京时间自然日 0 点自动归零。

**n = 0（5 条）：**
```
今日零进度，任务簿都为你面红。
一个都未搞掂，你系咪同任务有仇呀？
咁hea，任务等到颈都长埋。
零蛋！再唔动手，任务都嫌你废。
今日挂住叹世界？任务喺度喊紧。
```

**n > 0（5 条，n 内插）：**
```
搞掂{n}个，难得你今日似个人。
{n}个落袋，算你今日冇白活。
做咗{n}个，犀利过平时啫。
今日{n}个收皮，少有嘢。
搞掂{n}个，终于唔系zero。
```

---

## 4. UI 设计规范

### 4.1 字体

- 暗色主题：`FusionPixel`（等宽），亮色主题：`StardewPixel`（比例），fallback `"SimSun","NSimSun","Microsoft YaHei",serif`。
- `@font-face` 每个字体注册 **2 个子集**（同名 family）：
  - latin：`unicode-range:U+0000-00FF,U+2000-206F,U+20A0-20CF,U+2190-21FF`
  - 汉字：`unicode-range:U+2E80-303F,U+3040-30FF,U+3400-4DBF,U+4E00-9FFF,U+AC00-D7AF,U+F900-FAFF,U+FF00-FFEF`
- 字体文件（woff2，外置 `fonts/` 目录）：
  - `fusion-pixel-12px-monospaced-latin.otf.woff2` / `fusion-pixel-12px-monospaced-zh_hans.otf.woff2`
  - `fusion-pixel-12px-proportional-latin.otf.woff2` / `fusion-pixel-12px-proportional-zh_hans.otf.woff2`
- **点阵字铁律**：基础字号 12px；禁用 bold、禁用 letter-spacing；`-webkit-font-smoothing:none`；任务文字 14px 是既有例外（暗色 `.text`、`.note` 均 14px）。

### 4.2 暗色主题（默认，DNF 风）

CSS 变量：
```css
:root{
  --gold:#e5b52f; --gold-hi:#ffe27a; --blue:#00b8e8; --orange:#ff8b10;
  --text:#d7c99c; --muted:#7e7c70;
  --c-main:#ffa94d; --c-side:#d7c99c; --c-done:#6e6c63;
  --font:"FusionPixel","SimSun","NSimSun","Microsoft YaHei",serif;
}
```

关键风格：

- **body 背景**：`radial-gradient(ellipse 120% 80% at 50% -10%,#182636,#0a0f14 50%,#04060a 78%,#020304)`。
- **main 外框**：`border:1px solid #0a0d10`，背景 `linear-gradient(90deg,#040505,#10110e,#040505)`，阴影 `0 0 0 1px #12324f,0 0 0 4px #030405,inset 0 0 0 1px #1d3f5f`。
- **titlebar**：高 36px、字号 24px，蓝渐变 `linear-gradient(#1a5696,#072a60 52%,#041a3c)`，金色文字 `var(--gold-hi)`，阴影 `1px 1px #000,0 0 8px #b78609`；左右各一个 7px 金色菱形（`::before/::after` rotate 45°）。右上角 `.close` × 装饰（无功能，仅悬停变色）。
- **quote**：居中 12px 金色 `#c9a94f`，上下渐变暗底，左右各 4px 菱形装饰。
- **rail**：深蓝黑竖向渐变，右 1px 边线；内部为 flex 纵向（头像→导航→主题按钮推底 `margin-top:auto`）。
- **crest 头像**：`width:min(126px,80%)`，`aspect-ratio:1/1`，1px 金边，径向渐变 `#3e2916→#12100c 58%→#030404` 底。
- **导航 tab**：
  - `.tab-main`（进行中/已完成）：暗金渐变底、1px 边 + 3px 左金边；active 态变蓝框蓝光（`border-color:#2a6f8f;border-left-color:var(--blue)`，内阴影 `#006a88`）。
  - `.tab-sub`（主线/支线/日期）：透明底、2px 左暗金边、金色文字；active 态亮金 + 左蓝边。
- **content-title**：高 30px 暗金横条，前置 6px 金菱形，后置渐隐 1px 横线。
- **list**：`border-top:2px solid #38382e`，无其它边框。
- **todo 行**：高 ≥52px flex，底部分隔线 `#2b2b24`；左侧 3px `::before` 竖条——主线 `var(--orange)` 带橙光晕、支线透明、已完成 `#26261f`、hover 暗金 `#6b5a24`、selected 金色发光。
  - checkbox：自定义 15px 方块（appearance:none），选中时用 data-URI SVG 金色对勾 + `#1c1a0e` 底。
  - `.text` 14px 带黑投影；`.mainline .text` 用 `var(--c-main)` 橙；`.done .text` `#6e6c63` 加删除线。
  - `.note` 备注 14px 青蓝 `#7ca2b5`、左 1px 竖线分隔。
  - `.sort-handle`：右侧六点拖拽手柄（内联 SVG，24px 字号容器，cursor:grab/grabbing），仅进行中任务显示。
  - selected 态：背景 `linear-gradient(90deg,#12201d,#182a20 68%,#0d1410)` + `inset 3px 0 0 var(--gold)` 金光。
- **add-todo-btn**：虚线 1px 暗金边框按钮，悬停变亮金。
- **composer 抽屉**：与 detail-body 同款"凹槽"卡片（1px 边 + 2px 外描 outline、inset 0 0 22px #000 内阴影）；默认 `display:none;opacity:0;transform:translateY(80px)`，`.open` 时 `display:block;opacity:1;transform:translateY(0)`，过渡 `opacity .3s ease-out, transform .3s ease-out`。
- **表单**：`.new-todo` 为 `grid-template-columns:1fr 100px`（任务 + 备注）；输入框暗底内阴影；`.major-picker` 单选做成按钮组（input 绝对定位隐藏，label 描边，主线选中变橙、支线选中变暗金）；`.add` 提交按钮为金渐变 `linear-gradient(#ffe080,#c9991f 45%,#9a6f10)` + 底部 2px 黑边，active 下沉 1px。
- **detail-panel**：左 1px 边线，纵向 flex；`.detail-head` 暗金横条显示选中任务名（主线橙/支线米/已完成灰，由 JS 内联 color）；`.detail-body` 凹槽卡片内含 hint、textarea（flex:1 填满，禁用 resize）、底部 `.detail-actions`（编辑/删除按钮，未选中时 `opacity:.45` + disabled，选中后 `.on` 亮起）。
- **编辑弹窗**：`.modal-overlay` 全屏半透明黑 `rgba(0,0,0,.62)` 居中；`.modal-panel` 400px 凹槽卡片，内含与新建一致的输入 + 单选 + `取消`(ghost) / `确认修改`(add)。

### 4.3 亮色主题（星露谷风，`body.light` 全覆盖）

新增/覆盖变量：
```css
body.light{
  --text:#4a3520; --gold:#e8a33d; --gold-hi:#ffd98a; --blue:#79a24b; --orange:#d98a1f;
  --muted:#a08d72; --c-main:#a76e17; --c-side:#5f8538; --c-done:#9a8f78;
  --wood:#8a5a2c; --wood-dk:#6d4420; --wood-lt:#c89a58;
  --parch:#fdf4dd; --ink:#4a3520; --leaf:#79a24b; --leaf-dk:#5f8538;
  --font:"StardewPixel","FusionPixel","SimSun","NSimSun","Microsoft YaHei",serif;
}
```

风格转换规则（所有组件都要覆盖，与暗色一一对应）：

- **body 背景**：`radial-gradient(ellipse 100% 55% at 50% -8%,#edf5da 0%,transparent 60%),linear-gradient(#f6ecd4,#ead9b4 58%,#dfcba0)`（田园奶油渐变）。
- **main**：羊皮纸 `--parch` 底 + 3px 木框 + 圆角 10px + 外发光白边与投影（`0 0 0 3px #fff7e6,0 6px 0 rgba(122,82,48,.32),0 14px 26px rgba(90,60,30,.16)`）。
- **titlebar**：木纹渐变 `linear-gradient(var(--wood-lt),var(--wood) 55%,#74471f)`，奶白文字，菱形装饰改 10px 圆角 2px 金块。
- **quote**：奶油底 + 棕字 `#6d4a26`，装饰改 7px 绿叶圆点。
- **通用质感**：所有"凹槽卡片"（detail-body / composer / modal-panel）改为 `border:2px solid #d3b882;border-radius:9px;background:linear-gradient(#fdf6e2,#f7ead0)` + 顶部内高光；所有按钮统一 `border:2px solid var(--wood-dk)` + 木纹渐变 + 圆角 7px + 底部 3px 投影，active 下沉 2px。
- **导航 tab**：`.tab-main` 木纹按钮，active 变金渐变（`--gold-hi→--gold→#c77f24`）深棕字；`.tab-sub` 奶油小卡片（1px 边、圆角 6px、左 16px 缩进），active 变金渐变。
- **content-title**：木纹横条、46px 高、24px 奶白大字，前 9px 金块，后金色虚线（`repeating-linear-gradient`）。
- **list**：奶油底 `#f4e8cc` + 2px 边 + 圆角 9px + 内边距 4px。
- **todo 行**：奶油底 `#fdf6e2`、圆角 7px、每行 2px 间距；左竖条 5px 绿叶渐变（主线改金渐变）；checkbox 16px 圆角 4px，选中橙边 + 奶油底 + 橙色对勾。
- **文字**：`.text`/`.note` 14px 无投影；done 态文字 `#9a8f78`。
- **detail 区**：hint 棕灰 `#a08d72`；textarea 奶油底 + 2px 金边圆角 6px；操作按钮奶油小按钮。
- **弹窗遮罩**：`rgba(60,45,20,.55)`。
- **主题按钮图标**：暗色主题显示太阳（`.ico-moon{display:none}`）、亮色显示月亮（`body.light .ico-sun{display:none}; body.light .ico-moon{display:inline-block}`）。
- **窄屏媒体查询同步覆盖**：`body.light .rail` 与 `body.light .detail-panel` 的边框方向随布局变为上下。

> ⚠️ **双主题铁律**：任何新增元素/样式都必须写两套（暗色默认 + `body.light` 覆盖）；JS 中一律用 CSS 变量（`--c-main/--c-side/--c-done` 等）而非硬编码色值。

---

## 5. 交互与状态机

### 5.1 全局状态

```js
const KEY='light-todo-v4';          // localStorage 任务数据 key
const THEME_KEY='light-todo-theme'; // localStorage 主题 key
let data = {todos: []};             // 内存中的全部任务
let view = 'active';                // 当前视图：active|main|side|done|done:{date}
let sel  = null;                    // 当前选中任务 id
let theme = 'dark' | 'light';
```

任务对象结构：
```js
{
  id: crypto.randomUUID(),
  text: string,        // ≤15 字
  note: string,        // ≤5 字，可空
  detail: string,      // 详情大段文字
  major: '主线' | '支线',
  done: boolean,
  completedAt: string|null,  // 完成时的 zh-CN 日期
  order: number        // 同 major 内的排序序号
}
```

### 5.2 视图切换（nav）

- 导航由 JS 动态渲染进 `#nav`：`进行中任务`（tab-main, data-view=active）、`主线任务`（tab-sub, main）、`支线任务`（tab-sub, side）、`已完成任务`（tab-main, done）+ 每个有完成任务的日期一个 tab-sub（`done:YYYY/MM/DD`，按日期倒序）。
- 点击 tab 切换 `view` 并 `sel=null; render()`；点 `已完成任务` 自动落到最近一个完成日期，无完成记录则回 active。
- `render()` 按 view 过滤列表：
  - active：`[...open('主线'), ...open('支线')]`（open = 未完成且按 order 升序）
  - main/side：对应 major 的 open 列表
  - done:{date}：当天完成的任务
- 已完成视图下：**硬隐藏** `#addTodoBtn` 并强制收起 composer（不播动画）、隐藏 `.detail-actions`、textarea 禁用且占位符变为只读提示。

### 5.3 任务 CRUD

- **新建**：点 `添加新任务` → composer 从底部 80px 淡入（0.3s），按钮文案变 `暂不添加`、十字图标竖线隐藏变 −；再点或提交成功后收起（收起动画同样 0.3s，结束后 `display:none`）。提交：校验 text 非空 → push 新任务（order=当前同 major 未完成数）→ 清空输入 → 收起 → render。
- **勾选完成/取消**：checkbox change → 写 `done` 与 `completedAt`（完成记当天日期，取消清空）；若在已完成视图取消勾选，自动跳回 active 视图。
- **选中**：点击任务行（非 checkbox、非拖拽手柄）→ `sel` 切换（再点取消）→ 行加 `.selected`，详情区显示该任务。
- **编辑**：选中后 `编辑` 按钮亮起 → 打开 modal 回填 text/note/major → 确认修改后 render；点击遮罩或按 Esc 或点取消关闭。
- **删除**：选中后 `删除` → `confirm('确定要删除这项任务吗？')` → 删除并 render。
- **详情编辑**：textarea `oninput` 即时写入 `t.detail` 并 `save()`（自动保存）；已完成视图禁用。
- **拖拽排序**：进行中任务右侧手柄可拖（HTML5 drag），仅在同一 major 组内、非已完成视图生效；dragover 时按鼠标在行上半/下半决定 insertBefore 位置；drop 后按 DOM 顺序重写该 major 组所有任务的 `order` 并 render。

### 5.4 主题切换

- 点击 `#themeToggle` → `applyTheme(theme==='light'?'dark':'light')`：切 `body.light` class、更新按钮文案（反显）、写 localStorage、同步替换 favicon data URI（DARK_ICON / LIGHT_ICON 两个内联 SVG 常量）。
- 页面加载时读 `localStorage[THEME_KEY]`（默认 dark）并 applyTheme。

### 5.5 每日完成数

- `renderDailyStat()` 在每次 render 后执行：统计 `done && completedAt===today()` 数量，从对应文案数组随机取一条写入 `#dailyStat`。

---

## 6. 数据存储与同步

### 6.1 双层存储

1. **localStorage（兜底/离线）**：key `light-todo-v4`，值为 `JSON.stringify(data)`。
2. **本地同步服务（主存储）**：`http://127.0.0.1:8765/data` 读写同目录 `data.json`。

### 6.2 JS 同步逻辑

```js
const SYNC_URL='http://127.0.0.1:8765/data';
function syncPush(){                    // 每次 save 后异步上传，失败静默
  fetch(SYNC_URL,{method:'POST',headers:{'Content-Type':'application/json'},
    body:JSON.stringify(data)}).catch(()=>{});
}
save = () => { localStorage.setItem(KEY, JSON.stringify(data)); syncPush(); };

function syncPull(){                    // 启动时拉取
  fetch(SYNC_URL,{cache:'no-store'}).then(r=>r.ok?r.json():null).then(remote=>{
    if(remote && remote.todos && remote.todos.length){
      data = remote;                    // 远端非空：以文件为准覆盖本地
      localStorage.setItem(KEY, JSON.stringify(data));
      render(); renderDailyStat();
    } else if(data.todos.length){
      syncPush();                       // 远端空而本地有数据：自动上传迁移
    }
  }).catch(()=>{});                     // 服务不可用：静默用 localStorage
}
// 启动顺序：applyTheme(theme); render(); syncPull();
```

### 6.3 server.py 规格（本地同步 + 静态服务）

- 纯标准库 Python（≥3.8），`http.server.HTTPServer`，监听 `127.0.0.1:8765`。
- `GET /data`：返回 `data.json` 原始字节（不存在则返回 `{"todos":[]}`）。
- `POST /data`：校验 JSON 为含 `todos` 键的对象 → 写入 `data.json.tmp` 后 `os.replace` **原子替换** → 返回 `{"ok":true}`；异常返回 400 + `{"ok":false,"err":...}`。
- `OPTIONS` 任意路径：204 + CORS 头。
- 其余 GET：静态文件服务（`/` 映射到 `/todolist-dnf.html`），带常见 MIME（html/css/js/json/woff2/png/svg/ico…），路径必须限制在 BASE 目录内防穿越。
- 所有响应带：`Access-Control-Allow-Origin: *`、`Allow-Methods: GET, POST, OPTIONS`、`Allow-Headers: Content-Type`、`Cache-Control: no-store`。
- 启动项（Windows）：启动文件夹放 `todo-sync.vbs`，用 `pythonw.exe` 无窗口运行 `server.py` 实现开机自启。

### 6.4 data.json 结构

```json
{"todos": []}
```
（除此结构外不包含任何预置任务数据。）

---

## 7. 技术约束与注意事项

1. **零依赖**：不引用任何 CDN / npm 包 / 框架；所有图标内联 SVG；字体仅 4 个本地 woff2。
2. **单文件交付**：最终产物为 `todolist-dnf.html` + `fonts/`（4 个 woff2）+ `server.py`（可选，用于文件同步）+ `data.json`（初始 `{"todos":[]}`）。
3. **浏览器兼容**：使用 `crypto.randomUUID()`、`:has()` 选择器（major-picker 选中态）、`aspect-ratio`、`fetch`，目标为现代 Chromium/Edge/Chrome。
4. **CSP 注意**：若经第三方预览面板托管，需允许 `connect-src http://127.0.0.1:*`（否则 syncPush/syncPull 被拦后静默回退 localStorage）。
5. **favicon**：`applyTheme` 时切换 `<link id="appIcon">` 的 href（两个 data-URI SVG 常量，暗色金框蓝黑底徽章、亮色木框奶油底徽章）。
6. **复刻验收清单**：
   - [ ] 三栏布局 + 窗口外框 + 标题栏"任务手册"
   - [ ] 顶部每日完成数粤语毒舌文案（0/非0 两组各 5 条随机）
   - [ ] 导航：进行中/主线/支线/已完成 + 动态日期子标签
   - [ ] 任务行：checkbox、文字（主线橙）、备注、拖拽手柄、选中金光
   - [ ] 底部抽屉式"接受新任务"表单（0.3s 淡入，按钮 +/− 切换）
   - [ ] 已完成视图：隐藏新建入口、详情只读、按日期分组
   - [ ] 详情区：自动保存 textarea + 编辑弹窗 + 删除确认
   - [ ] 暗色 DNF / 亮色星露谷双主题一键切换并持久化（含 favicon 联动）
   - [ ] localStorage + 8765 同步服务双层存储（服务缺席时静默降级）
   - [ ] `@media(max-width:1000px)` 单列堆叠（双主题同步）

---

## 8. 附：图标 SVG 清单（均需内联）

| 图标 | 用途 | 要点 |
|---|---|---|
| × | titlebar 右侧装饰 | 两笔交叉线，stroke 2 |
| 太阳/月亮 | 主题切换按钮 | 按主题互斥显示 |
| 十字 | 添加新任务按钮 | 竖线 `id="plusV"`，收起态隐藏竖线变 − |
| 六点手柄 | 拖拽排序 | 2 列 3 行圆点 r1.6 |
| 铅笔 | 编辑按钮 | 路径描边 1.4 |
| 垃圾桶 | 删除按钮 | 路径描边 1.4 |
| 对勾 | checkbox 选中 | data-URI 背景图（暗色金 `#e5b52f` / 亮色橙 `#c77f24`） |
| 徽章 | favicon 双版本 | 64×64 圆角方形 + 装订线 + 勾选框 + 三条任务杠 |

> 徽章 favicon 结构（两版仅配色不同）：外框 rx=13 描边 3 → 左侧装订竖条 → 勾选小方框 + 对勾 → 三条不等宽任务横杠（首条实色、其余 opacity .92/.78）。暗色版：底 `#1a2f47→#0b1526`、框 `#c9a94f`、勾选 `#ffd98a`、杠 `#d7c99c`；亮色版：底 `#fdf3d7→#f0ddb4`、框 `#8a5a2b`、勾选 `#4e8f3c`、杠 `#6b4a2a`。
